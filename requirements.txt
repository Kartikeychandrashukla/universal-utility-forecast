# Universal Utility Risk Analytics Platform
# Streamlit Cloud Requirements
# Compatible with Python 3.11, 3.12, and 3.13

# Core dependencies (always required)
numpy>=1.21.0,<2.0
pandas>=2.1.0
streamlit>=1.28.0
plotly>=5.17.0
scikit-learn>=1.3.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
matplotlib>=3.5.0
scipy>=1.8.0

# Note: Advanced forecasting models (ARIMA, Prophet) will be automatically
# disabled if they fail to install. The Simple Moving Average model will
# always be available and works without additional dependencies.
